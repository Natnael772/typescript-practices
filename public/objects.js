"use strict";
//objects
function greet1(person) {
    console.log(`Hello ${person.name}`);
}
function greet2(person) {
    console.log(`Hello ${person.name}`);
}
function greet3(person) {
    console.log(`Hello ${person.name}`);
}
